const exportStrings = async (glob: string, outputPath?: string) => {
  // This function should
  // Find all the translations in the files
  // Write it to .pot file
  // And update .po files next to .pot file
};

export default exportStrings;
